<h2><%= title %></h2>

<ul>
  <% items.forEach(item => { %>
    <li>
      <%= item.name %> — Category: <%= item.category_name %>
      <a href="/items/<%= item.id %>/edit">Edit</a>
      <form action="/items/<%= item.id %>?_method=DELETE" method="POST" style="display:inline;">
        <button type="submit">Delete</button>
      </form>
    </li>
  <% }) %>
</ul>
